{
  "name": "backend",
  "private": true,
  "scripts": {
    "dev": "venv/bin/python -m pip install -r requirements.txt --no-warn-script-location && venv/bin/python -m uvicorn src.main:app --reload",
    "test": "fastapi dev main.py"
  }
}
